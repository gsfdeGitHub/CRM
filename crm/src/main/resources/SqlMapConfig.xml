<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--读取JDBC.properties文件的属性-->
    <!--<properties resource="JDBC属性配置文件的路径"></properties>-->

    <!--设置日志输出语句，显示相应操作的sql语句-->
    <!--设置懒加载的全局设置，默认是false关闭的-->
    <settings>
        <setting name="logImpl" value="STDOUT_LOGGING"/>
        <setting name="lazyLoadingEnabled" value="true"/>
    </settings>
    <!--注册实体类的别名-->
    <!--<typeAliases>
        <package name="实体类的全限定包名"/>
    </typeAliases>-->
    <!--配置环境变量-->
    <!--<environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"></transactionManager>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/mybatis"/>
                <property name="username" value="root"/>
                <property name="password" value="root"/>
            </dataSource>
        </environment>
    </environments>-->

    <!--注册mapper.xml文件-->
    <!--<mappers>
        &lt;!&ndash;单个注册<mapper class="com.gsf.mapper.UsersMapper"></mapper>&ndash;&gt;
        &lt;!&ndash;批量注册，将com.gsf.mapper中的mapper.xml文件统统注册&ndash;&gt;
        <package name="mapper接口所在的包名"/>
    </mappers>-->

</configuration>